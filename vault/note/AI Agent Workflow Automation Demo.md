---
created: '2025-12-01'
description: Tutorial snippet demonstrating how to configure and execute an AI agent
  node in a workflow automation tool (likely n8n) to summarize news and send daily
  recap emails
janitor_note: LINK001 — Telia'z AI Clinical Platform wikilink is valid (YAML escaping
  false positive). Base view embed (related.base#All) references non-existent _bases/related.base
  — vault-wide issue. ORPHAN001 — no inbound wikilinks; consider linking from project/Telia'z
  AI Clinical Platform.
name: AI Agent Workflow Automation Demo
project: '[[project/Telia''z AI Clinical Platform]]'
related:
- '[[person/Rami Khouri]]'
relationships: []
session: null
status: active
subtype: reference
tags: []
type: note
---

# AI Agent Workflow Automation Demo

## Context

Brief tutorial recording from December 1, 2025, demonstrating how to use an AI agent as an assistant within a workflow automation platform (likely n8n). The recording is a truncated snippet — only the opening introduction was captured.

## Key Points

- The demo shows how to configure an **AI agent node** within a workflow
- Use case demonstrated: summarize the latest news and send a daily recap email each morning
- The AI agent node has:
  - Custom **instructions** defining its behavior
  - A selected **model** for processing
  - Connected **tools** (in this case, tools to fetch news feeds)
- Workflow execution is triggered manually via "execute workflow" to test the agent in action

## Relevance

This pattern of embedding AI agents within automated workflows is directly applicable to Telia'z clinical platform automation goals — particularly for tasks like clinical summary generation, report compilation, and notification workflows.

## Source

Limitless recording, 2025-12-01, 11:35 UTC. Truncated — only opening ~15 seconds captured.

## Related
![[related.base#All]]


## Execution Demo (11:40 clip)

A follow-up recording captured ~20 seconds later continues the demo, showing the actual workflow execution:

- The agent node had already been configured with instructions, model, and news feed tools (shown in the intro clip)
- The presenter clicks **"Execute Workflow"** to run the pipeline
- The workflow executes and the AI agent processes in real time
- The recording is truncated mid-execution — no output or results were captured

This confirms the demo followed a configure-then-execute pattern: define the agent behavior first, then trigger the full pipeline.

### Source

Limitless recording, 2025-12-01, 11:40 UTC. Truncated — only ~20 seconds captured.


## Workflow Execution

A follow-up segment (11:40 UTC) continues the demo by executing the configured workflow:

- Instructions and model were already configured in the AI agent node (shown in the intro segment)
- Tools to fetch news feeds were connected
- Clicking **"Execute Workflow"** runs the entire pipeline, allowing observation of the agent in action
- The demo walks through clicking the execute button and watching results flow through

## Source (Part 2)

Limitless recording, 2025-12-01, 11:40 UTC. Truncated — only ~20 seconds of execution walkthrough captured.
